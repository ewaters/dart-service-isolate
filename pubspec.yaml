name: service_isolate
description: A protobuf-based service isolate
publish: none

environment:
  sdk: '>=2.14.4 <3.0.0'

dependencies:
  protobuf: ^2.0.0
  change_case: any
  stream_channel: ^2.1.0
  protoc_builder:
    git: https://github.com/ewaters/dart-protoc-builder.git

dev_dependencies:
  build_runner: ^2.1.5
  lints: ^1.0.0
